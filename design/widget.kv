<KIOverlayWidget>:
    id: kiOverlay

    MDBoxLayout:
        orientation: "vertical"
        size: root.size
        pos_hint: {'center_x': .5, 'center_y': 0.9}
        spacing: "30px"

        MDSpinner:
            size_hint: None, None
            size: dp(50), dp(50)
            active: True
            pos_hint: {'center_x': .5, 'center_y': 0.5}

        MDRoundFlatButton:
            text: "Vergleiche mit KI"
            on_press: root.hit_compare = True
            pos_hint: {'center_x': .5, 'center_y': 0.5}

<KIImageWidget>    
    MDBoxLayout:
        orientation: "vertical"
        size: root.size
        id: kiImage 

        MDLabel:
            text: root.prediction
            size_hint: 1,0.2

<UserImageWidget>
    MDBoxLayout:
        orientation: "vertical"
        size: root.size
        id: userImage 

        MDLabel:
            text: root.user_prediction
            size_hint: 1,0.2

<UserAnnotationsWidget>:
    MDBoxLayout:
        orientation: "vertical"
        id: userAnnotations
        size: root.size
        
        MDLabel:
            text: "Was siehst du?"
            halign: "left"
            role: "large"
            size_hint: 1,0.1

        MDTextField:
            id: userInput
            hint_text: "Tippe ein was du siehst"
            size_hint: 1,0.2

        MDLabel:
            text: "Markiere die Bildbereiche"
            halign: "left"
            size_hint: 1,0.1
